<UserControl x:Class="StreamChatApp.View.HostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StreamChatApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:resx="clr-namespace:StreamChatApp.Properties"
             DataContext="{Binding HostViewModel, Source={StaticResource ViewModelLocator}}"
        >
    <UserControl.Resources>
        <Style x:Key="StackPanelInners" TargetType="{x:Type Control}">
            <Setter Property="Margin" Value="10" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
            </StackPanel>

            <StackPanel Grid.Column="1" >
                <Button Content="Start" Command="{Binding StartHost}" />
                <Button Content="Stop" Command="{Binding StopHost}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
